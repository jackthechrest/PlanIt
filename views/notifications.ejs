<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/style.css" />
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notifications | PlanIt </title>
</head>
<body>
    <div class="navbar">
        <a href="/logo" class="logo"><img src="/imgs/logo.png" alt="PlanIt Logo"></a>
        <a href="/messages" class="menu"><img src="/imgs/inbox_icon.png" alt="Inbox (No Notifications)"></a>
        <a href="/search" class="menu"><img src="/imgs/search_icon.png" alt="Search"></a>
        <a href="/calendar" class="menu"><img src="/imgs/calendar_icon.png" alt="Calendar"></a>
    </div>

    <h1 id="navbarEnd">INBOX MENU</h1>
    <ul>
        <li>
            <p>- <a href="/messages">Messages</a></p>
        </li>
        <li>
            <p>- <a href="/notifications">Other Notifications</a></p>
        </li>
        <% if (user.isAdmin) { %>
            <li>
                <p>- <a href="/reports">Reports</a></p>
            </li>
        <% } %>
    </ul>

    <h1>Other Notifications</h1>
        <% if (notifications.length === 0) { %>
            <p>No Notifications</p>
        <% } else { %>
            <ul>
                <% for (const notification of notifications) { %>
                    
                    <% if (notification.type === "FRIEND REQUEST SENT") { %>
                    <li>
                        <p>[<%=notification.dateString%>]  <a href="<%=notification.link%>"><%=notification.sendingUsername%> sent you a friend request!</a></p>
                        <% if (!notification.respondedTo) { %>
                            <button><a href="/api/friend/<%=notification.sendingUserId%>/ACCEPT">Accept</a></button>
                            <button><a href="/api/friend/<%=notification.sendingUserId%>/DECLINE">Decline</a></button>
                        <% } %>
                    </li>
                    <% } else if (notification.type === "FRIEND REQUEST ACCEPTED") { %>
                        <li>
                            <p>[<%=notification.dateString%>]  <a href="<%=notification.link%>"><%=notification.sendingUsername%> accepted your friend request!</a></p>
                        </li>
                    <% } else if (notification.type === "FOLLOW") { %>
                    <li>
                        <p>[<%=notification.dateString%>]  <a href="<%=notification.link%>"><%=notification.sendingUsername%> followed you!</a></p>
                    </li>
                    <% } else if (notification.type === "WARNING") { %>
                        <% if (notification.sendingUsername === "JackTheChrest") { %>
                        <li>
                            <p>[<%=notification.dateString%>] Your profile features offensive content and has been modified.  Don't do it again.</p>
                        </li>   
                        <% } else if (notification.sendingUsername === "Quinn") { %>
                        <li>
                            <p>[<%=notification.dateString%>]  Your event features offensive content and has been modified.  Don't do it again.</p>
                        </li>   
                        <% } else if (notification.sendingUsername === "Matthew") { %>
                        <li>
                            <p>[<%=notification.dateString%>]  You posted an offensive wall post/comment, which has been removed.  Don't do it again.</p>
                        </li>   
                        <% } %>
                    <% } else if (notification.type === "EVENT CREATED" || notification.type === "EVENT EDITED" || notification.type === "EVENT CANCELLED") { %>
                    <li>
                        <p>[<%=notification.dateString%>]  <a href="<%=notification.link%>">Event</a></p>
                    </li>
                    <% } %>
                <% } %>
            </ul>
            <% } %>
</body>
</html>