<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/style.css" />
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= user.username %> Following | PlanIt</title>
</head>
<body>
    <h1>MENU</h1>
    <ul>
        <li>
            <p>- <a href = "/logo">Logo</a></p>
        </li>
        <li>
            <p>- <a href="/users/<%= user.userId %>/calendar">Calendar</a></p>
        </li>
        <li>
            <p>- <a href="/search">Search</a></p>
        </li>
        <li>
            <p>- <a href="/users/<%= user.userId %>/messages">Inbox</a></p>
        </li>
    </ul>

    <h1 class = "follow-header"><%= user.username %> Following:</h1>

    <% if (user.following.length === 0) { %>
    <p>Not Following Anyone</p>
    <% } else { %>
    <ul>
        <% for (const followed of user.following) { %>
        <li>
            <p>- <a href="/users/<%= followed.targetUserId %>"><%= followed.targetUsername %></a></p>
        </li>
        <% } %>
    </ul>
    <% } %>

</body>
</html>