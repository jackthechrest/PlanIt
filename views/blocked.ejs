<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/style.css" />
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= user.username %> Blocked Users | PlanIt</title>
</head>
<body>
    <div class="navbar">
        <a href="/logo" class="logo"><img src="/imgs/logo.png" alt="PlanIt Logo"></a>
        <a href="/messages" class="menu"><img src="/imgs/inbox_icon.png" alt="Inbox (No Notifications)"></a>
        <a href="/search" class="menu"><img src="/imgs/search_icon.png" alt="Search"></a>
        <a href="/calendar" class="menu"><img src="/imgs/calendar_icon.png" alt="Calendar"></a>
    </div>

    <h1 class = "follow-header" id="navbarEnd"><%= user.username %> Blocked Users:</h1>

    <% if (blockedUsers.length === 0) { %>
    <p>No Blocked Users</p>
    <% } else { %>
    <ul>
        <% for (const blocked of blockedUsers) { %>
        <li>
            <p>- <a href="/users/<%= blocked.userId %>"><%= blocked.username %></a></p>
        </li>
        <% } %>
    </ul>
    <% } %>

</body>
</html>